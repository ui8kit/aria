# Contributing

- Keep compatibility by preserving `data-ui8kit-*` contracts.
- Add a new pattern by creating `src/patterns/<name>/index.ts`, `src/patterns/<name>/markup.md`, and tests.
- Export subpath in `package.json` and `src/entries/<name>.ts`.
- Add docs in `docs/patterns/<name>.md` and fixture examples.
